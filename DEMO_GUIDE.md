# 演示流程指南

本文档用于现场演示"国网江苏市级配电网智能成票与安全校验系统"的完整功能。

---

## 启动系统

**Windows:**
```bash
start.bat
```

**Mac/Linux:**
```bash
chmod +x start.sh
./start.sh
```

**或分别启动：**
```bash
# 终端1：后端
cd backend
pip install -r requirements.txt
uvicorn main:app --reload

# 终端2：前端
cd frontend
npm install
npm run dev
```

浏览器打开：**http://localhost:5173**

---

## 演示步骤

### 步骤 1：系统主界面

访问 http://localhost:5173

页面展示：
- **6 个指标卡片**：今日故障事件、待成票任务、安全校验通过率、高危馈线数、待人工复核、已导出操作票
- **IEEE 33 节点拓扑图**：交互式 SVG 配电网图，含 33 个母线节点、断路器、分段开关、联络开关
- **右侧当前事件面板**：演示场景 SC-2026-0705-001
- **底部风险馈线表**：F12/F21/F09/F05 馈线风险等级
- **业务流程**：8 步横向流程图

### 步骤 2：交互式拓扑图

在拓扑图上操作：
1. **悬停设备** → 显示 tooltip（设备名称、类型、状态）
2. **点击 Bus 8 或 Bus 9** → 右侧显示设备详情面板
3. **点击 Tie 2 (9-15 联络开关)** → 查看联络开关状态
4. **点击「标记故障区段」按钮** → Bus 8-9 区段变红高亮
5. **点击「高亮推荐转供路径」按钮** → Bus 9→Tie 2→Bus 15 变蓝高亮
6. **点击「清除高亮」按钮** → 恢复原始显示

### 步骤 3：从拓扑图进入边界判定

1. 点击拓扑图中的 **Bus 9** 节点
2. 在右侧详情面板中点击 **「以该设备作为故障点进行边界判定」**
3. 自动跳转到**边界判定页面**，故障设备已自动填充为 BUS-09

### 步骤 4：边界判定

边界判定页面（左右分栏布局）：
1. 左侧「故障输入」卡片确认故障信息
2. 点击 **「开始边界判定」**
3. 右侧「判定结果」卡片显示：
   - 故障区段：Bus 8 - Bus 9
   - 跳闸断路器：CB101
   - 隔离开关：DS12、DS18
   - 失电负荷：2.4 MW
   - 边界结论：建议隔离后转供

### 步骤 5：转供决策

点击左侧菜单「转供决策」，或从边界判定页继续：
1. 可看到顶部「边界判定摘要」
2. 点击 **「执行转供决策」**
3. 查看候选路径对比表（3 条路径）
4. 右侧「推荐方案」卡片（绿色边框突出）显示：
   - 方案编号、候选路径、转供负荷、负载率
   - N-1 结果：通过 | FA 结果：一致
   - 综合评分：87.5
   - 推荐理由
5. 点击 **「采用推荐方案」**

### 步骤 6：操作序列生成

点击左侧菜单「操作序列生成」：
1. 顶部显示 5 个阶段标签（故障确认→安全隔离→负荷转移→恢复供电→方式确认）
2. 点击 **「生成操作序列」**
3. 上方摘要显示：**"共生成 6 个操作步骤，其中 2 个需要人工确认"**
4. 查看步骤表格：
   - 步骤 1：确认 CB101 分位
   - 步骤 2-3：拉开隔离开关
   - 步骤 4：合上联络开关 TS201
   - 步骤 5-6：确认状态
   - 人工确认项用黄色标记

### 步骤 7：模板化成票

点击左侧菜单「模板化成票」：
1. 点击 **「生成操作票」**
2. 左侧显示操作票预览（票据样式）：
   - 票号：OT20260705-XXX
   - 操作任务：10kV城中馈线#15杆分段开关故障隔离及负荷转供
   - 操作步骤表：含序号、阶段、操作、设备、内容、安全提示
3. 右侧显示版本信息（V1.0、草稿状态）

### 步骤 8：人工修改操作票（可选）

1. 在操作票预览中点击某个步骤的**操作内容**列
2. 弹出编辑窗口，修改内容
3. 点击「保存修改」
4. 版本号自动递增（V1.0 → V1.1），状态变为「已校核」

### 步骤 9：安全校验

点击左侧菜单「安全校验」：
1. 操作票编号自动填充
2. 点击 **「执行安全校核」**
3. 页面展示：
   - **3 张结果统计卡**：通过项(绿)、警告项(黄)、阻断项(红)
   - **阻断提示横幅**：存在阻断项时红色提示
   - **8 条校验明细**：每条可点击展开

### 步骤 10：查看校验详情

在安全校验结果中：
1. **点击「容量约束校验」（警告项）** → 展开详情
   - 风险原因：城东馈线转供后负载率达 78.5%
   - 处理建议：建议缩短转供持续时间
   - 关联步骤：第 4 步
2. **点击「关联步骤 4」** → 跳转到模板化成票页面第 4 步
3. **点击「确认警告项」** → 填写确认人姓名 → 状态更新为「已人工确认」

### 步骤 11：阻断项处理

在安全校验结果中：
1. **点击「五防互锁校验」（阻断项）**
2. 展开后显示：**"🚫 阻断项不得人工直接放行"**
3. 无「确认」按钮，需返回修改操作票

### 步骤 12：导出 Excel 操作票

在模板化成票页面：
1. 点击 **「📥 导出 Excel」**按钮（绿色）
2. 浏览器下载文件：`操作票_OT20260705-XXX_20260705xxxxxx.xlsx`
3. 打开 Excel 文件验证：
   - **Sheet 1**：操作票基本信息（票号、任务、运行方式、时间、版本等 9 项）
   - **Sheet 2**：操作步骤（序号、阶段、对象、动作、内容、安全提示等 7 列）
   - **Sheet 3**：安全校验结果（类型、结果、原因、建议、确认人、时间等 6 列）

### 步骤 13：查看测试统计

点击左侧菜单「测试统计」：
1. 顶部 6 个指标卡（测试用例数、通过数、警告数、阻断数、一次通过率、人工修改比例）
2. **CSS 进度条**：通过/警告/阻断的分布比例
3. 操作票列表
4. 测试用例表（TC-001 ~ TC-004）
5. 点击 **「导出测试报告」**

### 步骤 14：一键演示（可选）

返回系统主界面：
1. 点击 **「🚀 一键生成演示流程」**
2. 系统自动执行：边界判定→转供决策→操作序列→成票→校核
3. 完成后提示：**"演示流程已生成！可前往各页面查看结果"**
4. 前往安全校验和模板化成票页面查看自动生成的结果

### 步骤 15：数据管理（可选）

点击左侧菜单「数据管理」：
1. 顶部 5 个数据状态卡片
2. 数据对象维护表（馈线模型、设备台账等 6 项）
3. 规则/模板维护表（4 项）
4. 数据导入流程（5 步编号流程）

### 步骤 16：源荷预测（可选）

点击左侧菜单「源荷预测风险」：
1. 5 条风险馈线列表
2. 按「高/中/低」筛选
3. 负载率颜色区分

---

## 演示提示

- **推荐演示顺序**：步骤 1 → 2 → 3 → 4 → 5 → 6 → 7 → 9 → 10 → 12 → 13
- **核心闭环**：拓扑点击 → 边界判定 → 转供决策 → 序列生成 → 成票 → 校核 → 导出
- **快速演示**：使用「一键演示」按钮自动生成全流程结果
- **界面亮点**：拓扑交互、三色校核、Excel 导出、进度条
